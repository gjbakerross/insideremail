<h1>New Email</h1>

<%= form_with model: @email do |form| %>
  <%= form.text_field :email %>
  <%= form.select(:country_id, @country_options) %>
  <%= form.submit %>
<% end %>

<%= link_to 'Back', emails_path %>
